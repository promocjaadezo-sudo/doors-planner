# Kopia wzorcowa dla `ai-agents/.env`
# Wypełnij i skopiuj do pliku `.env` zanim odpalisz managera agentów

# Klucz OpenAI wymagany przez MonitoringAgent (opcjonalnie możesz pominąć monitoring `--skip-monitoring`)
OPENAI_API_KEY=

# Identyfikatory projektu Firebase wykorzystywane przez aplikację Doors Planner
FIREBASE_PROJECT_ID=
FIREBASE_APP_ID=
FIREBASE_USER_ID=

# Dane konfiguracyjne aplikacji sieciowej (toczą się do state.storage.fbConfig w UI)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_MEASUREMENT_ID=

# Ścieżka do klucza serwisowego (plik `firebase-credentials.json` w katalogu głównym repozytorium)
FIREBASE_CREDENTIALS_PATH=../firebase-credentials.json

{
  "name": "doors-planner",
  "version": "0.1.0",
  "private": true,
  "description": "E2E tests for doors planner SPA using Playwright",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.1"
  }
}
